import { Module } from '@nestjs/common';

// Empty scaffold (Phase 0.1, build-guide.md) — async (@OnEvent) notification
// dispatch with delivery-status tracking lands here in Phase 7.
@Module({})
export class NotificationModule {}
