{{#> layouts/main}}
  <h1 class="page-title">{{title}}</h1>
  <dl class="resource-summary">
    <div><strong>Id</strong> {{submission.id}}</div>
    <div><strong>Status</strong> {{submission.status}}</div>
    <div><strong>Degree</strong> {{submission.degreeType}}</div>
  </dl>
  <p class="lede">Full supervisor workflow and presigned upload land in a later Phase 6 slice.</p>
{{/layouts/main}}
