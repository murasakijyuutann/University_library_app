import './portal.css';

/**
 * Small vanilla TypeScript entry for the portal (Phase 6).
 * HTMX is loaded from the layout CDN; this bundle owns CSS + future upload UX.
 */
document.documentElement.classList.add('js');
