import { Module } from '@nestjs/common';

// Empty scaffold (Phase 0.1, build-guide.md) — thesis submission + embargo
// workflow lands here in Phase 3+ (state machine, on top of AccessPolicyResolver).
@Module({})
export class ThesisModule {}
