// Placeholder for the UnifiedSearchService contract (Phase 5, build-guide.md;
// full spec in docs/search-interface-contract.md). This file exists now only
// to give the dependency-cruiser boundary rule (.dependency-cruiser.cjs,
// "search-api-no-persistence") something real to guard from day one, per
// Phase 0.5. Do not import anything from src/prisma or @prisma/client here —
// that import would fail the build by design.
export {};
